## 2024-05-31 - [Keyboard Accessibility Pattern]

**Learning:** PrimeVue `<Button>` components that are icon-only must always have an `aria-label` attribute.
**Action:** When adding or modifying interactive icon elements, explicitly define an `aria-label` for screen reader accessibility.
